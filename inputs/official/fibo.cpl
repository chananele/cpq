instructions  Fibo  /* print fibonacci sequence */
  variables a : int;
      b, c : int;

start

    a := 0;
    b := 1;

    while (a <= 1000) {

       print (a);
       c := a + b;
       a := b;
       b := c;
    }
	
end
